<?xml version="1.0"?>
<!DOCTYPE module PUBLIC
        "-//Checkstyle//DTD Checkstyle Configuration 1.3//EN"
        "https://checkstyle.org/dtds/configuration_1_3.dtd">

<!--
    Lightweight Checkstyle ruleset for Sports Manager.
    Enforces the most impactful style rules without being overly strict.
    Run via: mvn checkstyle:check
    Report:  mvn checkstyle:checkstyle  (target/site/checkstyle.html)
-->
<module name="Checker">

    <property name="charset" value="UTF-8"/>
    <property name="severity" value="warning"/>

    <!-- No tab characters -->
    <module name="FileTabCharacter">
        <property name="eachLine" value="true"/>
    </module>

    <!-- No trailing whitespace -->
    <module name="RegexpSingleline">
        <property name="format" value="\s+$"/>
        <property name="minimum" value="0"/>
        <property name="maximum" value="0"/>
        <property name="message" value="Line has trailing spaces."/>
    </module>

    <module name="TreeWalker">

        <!-- Imports -->
        <module name="UnusedImports"/>
        <module name="RedundantImport"/>
        <module name="AvoidStarImport"/>

        <!-- Naming conventions -->
        <module name="TypeName"/>          <!-- Classes/interfaces: UpperCamelCase -->
        <module name="MethodName"/>        <!-- Methods: lowerCamelCase -->
        <module name="LocalVariableName"/> <!-- Local vars: lowerCamelCase -->
        <module name="ConstantName"/>      <!-- Constants: UPPER_CASE -->

        <!-- Braces -->
        <module name="NeedBraces"/>        <!-- if/for/while must have braces -->
        <module name="LeftCurly"/>         <!-- { on same line -->

        <!-- Code quality -->
        <module name="EmptyBlock"/>        <!-- No empty catch/finally blocks -->
        <module name="SimplifyBooleanExpression"/>
        <module name="SimplifyBooleanReturn"/>
        <module name="StringLiteralEquality"/>  <!-- Use .equals(), not == -->
        <module name="EqualsHashCode"/>         <!-- Override both or neither -->

        <!-- Line length — allow up to 140 chars (realistic for Java) -->
        <module name="LineLength">
            <property name="max" value="140"/>
            <property name="ignorePattern" value="^package.*|^import.*|a href|href|http://|https://|ftp://"/>
        </module>

    </module>
</module>
